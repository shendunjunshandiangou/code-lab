---
title: SQL真题，轻松拿捏！
---

<BiliPlayer :bvid="'BV1ti9MBuEXZ'" />

> **摘要**：SQL 真题系列第一课，聚焦**表连接**——讲清为什么必须连表、LEFT JOIN 匹配过程，并通过三道基础真题实操。系列后续覆盖子查询、窗口函数、综合题。

## 一、系列规划

预计 3–4 节直播，按难度递进：

1. **表连接**（本节）
2. 子查询
3. 窗口函数
4. 综合性题目

## 二、为什么需要表连接

**核心目的：原表字段不够用，需要从另一张表补充字段。**

与 Excel 的区别：

| Excel | SQL |
| --- | --- |
| VLOOKUP 直接「劈」字段过来 | 没有直接取字段的手段，必须**先把表拼在一起**，再取字段 |

所以叫「表连接」而非「字段连接」。

## 三、表连接过程（以 LEFT JOIN 为例）

两张表通过**连接键**（如 `dept_id`）匹配：

1. 固定左表，取左表第一行
2. 用连接键值去右表**逐行扫描**匹配
3. 匹配成功 → 拼成新行；匹配失败 → 右表字段为 NULL（LEFT JOIN 保留左表行）
4. 重复直到左表所有行处理完

### 数据膨胀

右表同一连接键有多行时，左表一行会匹配出多行——这叫**简单版笛卡尔积**。原本 4 行可能膨胀为 6 行。

### JOIN 类型速览

| 类型 | 工作场景 |
| --- | --- |
| **LEFT JOIN** | **最常用**，保留左表全部行，未匹配右表为 NULL |
| INNER JOIN | 只保留两边都匹配的行，丢数据不符合多数业务 |
| RIGHT JOIN | 固定右表找左表 |
| CROSS JOIN | 笛卡尔积，特殊场景使用 |

> 工作中推荐只用 LEFT JOIN，其他了解即可。直接写 `JOIN` 默认为 INNER JOIN，容易丢数据。

## 四、做题前提：先看懂表

做不出 SQL 题，大概率是**表没看懂**——先理解每张表是什么粒度、有哪些字段、表之间什么关系。

## 五、子查询 vs 表连接

效率上子查询有时更好，但表连接更直观。实际选题看场景和个人习惯。

## 六、连续登录（预告）

连续登录属于**套路题**，本系列后续会讲。面试中常口述思路，不需要完整写出代码，但思路必须清晰。
